import { createTheme } from '@mantine/core';

export const themeFonts = createTheme({
  fontFamily:
    'Work Sans Variable, -apple-system, BlinkMacSystemFont, Segoe UI, Roboto, Helvetica, Arial, sans-serif, Apple Color Emoji, Segoe UI Emoji',
});
